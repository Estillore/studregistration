export const navBar = (appContainer) => {
  appContainer.innerHTML += `
    <nav class="navbar bg-body-light border-bottom">
      <div class="container-fluid">
        <h6 class="navbar-brand m-1" href="#">
          Bootstrap
        </h6>
      </div>
    </nav>
  `;
};
